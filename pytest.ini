[pytest]
testpaths = tests
python_files = test_*.py
asyncio_mode = auto
addopts =
    -ra
    --strict-markers
    --cov=dhan_telegram_bridge
    --cov=live_screener_main
    --cov-report=term-missing
    --cov-report=xml
